export enum ApiInternal {
  SAVE_LOGIN = "/api/auth/save",
  LOGOUT = "/api/auth/logout",
}

export enum RoutePages {
  LOGIN = "/",
  HOME = "/home",
  VENDA = "/venda",
  PRODUTO = "/produto",
  DASHBOARD = "/dashboard",
}
